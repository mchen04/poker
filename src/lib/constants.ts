/** Number of characters in a room/invite code (landing-page input + generator). */
export const ROOM_CODE_LENGTH = 6;

/** How long the host's "End & export" button waits for a confirm tap before resetting. */
export const END_GAME_CONFIRM_MS = 4000;

/** Transient toast lifetime (ms). */
export const TOAST_DURATION_MS = 3200;
